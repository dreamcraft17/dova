# =============================================================================
# DOVA VPS — Frontend env (only if Next.js also runs on the same VPS)
# Copy to VPS:  /var/www/dova/apps/frontend/.env.local
# =============================================================================
# Skip this file if frontend is deployed on Vercel — set vars there instead.

NEXT_PUBLIC_API_URL=https://api.dova.dntech.id/api/v1
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_test_REPLACE

# Change api.dova.dntech.id to match your nginx/API hostname.
