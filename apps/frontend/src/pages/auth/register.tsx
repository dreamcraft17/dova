import { FormEvent,useState } from 'react'; import { useRouter } from 'next/router'; import { Layout } from '../../components/Layout'; import { api } from '../../lib/api';
export default function Register(){const [form,setForm]=useState({fullName:'',email:'',password:'',confirmPassword:''});const [error,setError]=useState('');const router=useRouter();async function submit(e:FormEvent){e.preventDefault();try{await api('/auth/register',{method:'POST',body:JSON.stringify(form)});router.push('/auth/login')}catch(e){setError((e as Error).message)}}return <Layout><section className="form-page"><p className="eyebrow">JOIN THE NETWORK</p><h1>Create your account</h1><form onSubmit={submit}>{[['fullName','Full name','text'],['email','Email','email'],['password','Password','password'],['confirmPassword','Confirm password','password']].map(([key,label,type])=><label key={key}>{label}<input type={type} required value={form[key as keyof typeof form]} onChange={e=>setForm({...form,[key]:e.target.value})}/></label>)}<button className="button">Create account</button>{error&&<p className="error">{error}</p>}</form></section></Layout>}
