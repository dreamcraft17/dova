import { Layout } from '../components/Layout'; import { RequireAuth } from '../components/RequireAuth'; export default function Supplier(){return <Layout><RequireAuth roles={['supplier','admin']}><section className="form-page"><p className="eyebrow">SUPPLIER DASHBOARD</p><h1>Grow your reach.</h1><p>Supplier inventory and fulfilment tools are ready for approved suppliers.</p></section></RequireAuth></Layout>}
