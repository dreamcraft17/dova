import { Layout } from '../components/Layout'; import { RequireAuth } from '../components/RequireAuth'; export default function Admin(){return <Layout><RequireAuth roles={['admin']}><section className="form-page"><p className="eyebrow">ADMIN DASHBOARD</p><h1>Platform oversight.</h1><p>Review suppliers, products, users, and orders from one place.</p></section></RequireAuth></Layout>}
